<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mcit.admissionsystem">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MenuActivity" />
        <activity android:name=".activities.student.ListStudentActivity" />
        <activity android:name=".activities.professor.ListProfessorActivity" />
        <activity android:name=".activities.course.ListCourseActivity" />
        <activity android:name=".activities.coursestudent.ListCourseStudentsActivity" />
        <activity android:name=".activities.student.MaintainStudentActivity" />
        <activity android:name=".activities.course.MaintainCourseActivity" />
        <activity android:name=".activities.professor.MaintainProfessorActivity" />
        <activity android:name=".activities.coursestudent.MaintainCourseStudentsActivity" />
        <activity android:name=".activities.coursestudent.AddStudentToCourseActivity"></activity>
    </application>

</manifest>